---
apiVersion: v1
kind: Service
metadata:
  name: gatus-service
  namespace: gatus
  labels:
    app: gatus
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: gatus
