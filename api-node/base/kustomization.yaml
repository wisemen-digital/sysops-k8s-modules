---
configMapGenerator:
  - name: configmap-api
    envs:
      - config/env.properties
resources:
  - deployment.yaml
  - service.yaml
labels:
  - pairs:
      app: api
      stack: node
      tier: backend
    includeTemplates: true
