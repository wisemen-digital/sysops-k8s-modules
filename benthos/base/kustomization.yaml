---
configMapGenerator:
  - name: configmap-benthos
    envs:
      - config/env.properties
resources:
  - deployment.yaml
images:
  - name: default-benthos
    newName: docker.redpanda.com/redpandadata/connect
    newTag: latest
labels:
  - pairs:
      app: benthos
      service: benthos
      tier: service
    includeTemplates: true
